---
layout: default
---
<div class="post about-page">
  <article>
    {% if page.profile %}
      <div class="profile profile-top">
        {% if page.profile.image %}
          {% assign profile_image_path = page.profile.image | prepend: 'assets/img/' %}
          {% assign profile_image_class = 'img-fluid profile-img' %}
          {% capture sizes %}(min-width: {{ site.max_width }}) {{ site.max_width | minus: 30 | times: 0.3}}px, (min-width: 576px)
      30vw, 95vw"{% endcapture %}
          <div class="profile-photo">
            {%
              include figure.liquid loading="eager" path=profile_image_path class=profile_image_class sizes=sizes alt=page.profile.image
              cache_bust=true
            %}
          </div>
        {% endif %}
        {% if page.profile.more_info %}
          <div class="more-info">{{ page.profile.more_info }}</div>
        {% endif %}
      </div>
    {% endif %}

    <div class="clearfix about-body">{{ content }}</div>

    <!-- News -->
    {% if page.announcements and page.announcements.enabled %}
      <h2>
        <a href="{{ '/news/' | relative_url }}" style="color: inherit">news</a>
      </h2>
      {% include news.liquid limit=true %}
    {% endif %}

    <!-- Latest posts -->
    {% if page.latest_posts and page.latest_posts.enabled %}
      <h2>
        <a href="{{ '/blog/' | relative_url }}" style="color: inherit">latest posts</a>
      </h2>
      {% include latest_posts.liquid %}
    {% endif %}

    <!-- Selected papers -->
    {% if page.selected_papers %}
      <h2>
        <a href="{{ '/publications/' | relative_url }}" style="color: inherit">selected publications</a>
      </h2>
      {% include selected_papers.liquid %}
    {% endif %}

    <!-- Social -->
    {% if page.social %}
      <div class="social">
        <div class="contact-icons">{% social_links %}</div>

        <div class="contact-note">{{ site.contact_note }}</div>
      </div>
    {% endif %}

    {% if site.newsletter and site.newsletter.enabled and site.footer_fixed %}
      {% if site.plugins contains 'al_newsletter' %}
        {% include plugins/al_newsletter_form_center_margin.liquid %}
      {% else %}
        {% include newsletter.liquid center=true %}
      {% endif %}
    {% endif %}
  </article>
</div>

<style>
  .about-page article {
    max-width: none;
  }

  .profile.profile-top {
    float: none;
    width: auto !important;
    max-width: 100%;
    margin: 0.25rem auto 2.25rem auto;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    flex-wrap: wrap;
    text-align: center;
  }

  .profile-photo {
    flex: 0 0 auto;
    width: 210px;
    max-width: 55vw;
    padding: 6px;
    border-radius: 2px;
    overflow: hidden;
    background: var(--paper-sheet-color, #faf7ee);
    border: 1px solid var(--ink-rule, rgba(26, 26, 23, 0.85));
    box-shadow:
      0 1px 2px rgba(26, 26, 23, 0.06),
      0 12px 28px rgba(26, 26, 23, 0.12);
  }

  .profile-photo figure {
    margin: 0;
    width: 100%;
    line-height: 0;
  }

  .profile-photo figure img,
  .profile-photo img {
    display: block;
    width: 100%;
    height: auto;
    border-radius: 0;
    box-shadow: none;
  }

  .profile.profile-top .more-info {
    flex: 0 1 16rem;
    text-align: center;
    margin: 0;
  }

  .profile.profile-top .more-info .profile-card {
    font-size: 0.92rem;
    line-height: 1.45;
    color: var(--global-text-color-light);
  }

  .profile.profile-top .more-info .profile-card > p {
    display: block !important;
  }

  .profile.profile-top .more-info .profile-card > p.profile-name {
    font-family: var(--font-display, "Playfair Display", serif);
    font-size: 2.7rem;
    font-weight: 800;
    letter-spacing: 0;
    color: var(--global-text-color);
    margin: 0 0 0.65rem 0 !important;
    line-height: 1.05;
  }

  .profile.profile-top .more-info .profile-card > p.profile-meta {
    margin: 0.2rem 0 !important;
  }

  .profile.profile-top .more-info .profile-card > p.profile-email {
    margin: 0.85rem 0 0.55rem 0 !important;
    padding-top: 0;
  }

  .profile.profile-top .more-info .profile-email a {
    font-weight: 500;
    text-decoration: none;
  }

  .profile.profile-top .more-info .profile-email a:hover {
    text-decoration: underline;
  }

  .profile.profile-top .more-info .profile-social {
    display: flex;
    justify-content: flex-start;
    gap: 0.95rem;
    margin-top: 0.35rem;
  }

  .profile.profile-top .more-info .profile-social a,
  .profile.profile-top .more-info .profile-social span {
    color: var(--global-text-color-light);
    font-size: 1.35rem;
    transition: color 0.2s var(--ease-out), transform 0.2s var(--ease-out);
    display: inline-flex;
  }

  .profile.profile-top .more-info .profile-social a:hover {
    color: var(--accent-color, #8a1a12);
    transform: translateY(-2px);
    text-decoration: none;
  }

  .profile.profile-top .more-info .profile-wechat {
    margin: 0.45rem 0 0;
    font-size: 0.78rem;
    opacity: 0.85;
  }

  .about-body {
    max-width: 40rem;
    margin: 0 auto;
    font-size: 1.05rem;
  }
</style>
